import type { Metadata } from "next";
import Link from "next/link";
import { getServerLang } from "./lib/i18n.server";

export const metadata: Metadata = {
  title: "Optiloves Invest",
  description: "Invest from $1 per token. Focus: Kinshasa & Luanda.",
};

const STRINGS = {
  en: {
    title: "Tokenized access to African real estate",
    cta: "View properties",
  },
  fr: {
    title: "AccÃ¨s tokenisÃ© Ã  l'immobilier africain",
    cta: "Voir les biens",
  },
} as const;

export default function Home() {
  const lang = getServerLang();
  const s = (STRINGS as any)[lang] ?? STRINGS.en;
  return (
    <main className="p-6">
      <section className="mx-auto max-w-4xl py-16 text-center">
        <h1 className="mb-4 text-4xl font-bold">{s.title}</h1>
        <Link
          href="/properties"
          className="inline-block rounded-lg bg-black px-4 py-2 text-white"
        >
          {s.cta}
        </Link>
      </section>
    </main>
  );
}
